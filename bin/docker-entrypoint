#!/bin/bash -e

# Se for o comando padrão do servidor, roda migrations antes
if [ "${1}" == "./bin/thrust" ] || [ "${1}" == "rails" ]; then
  ./bin/rails db:migrate 2>/dev/null || ./bin/rails db:prepare
fi

exec "${@}"